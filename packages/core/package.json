{
  "name": "@atjson/core",
  "version": "0.5.0",
  "description": "A brand new TypeScript library.",
  "main": "dist/src/index.js",
  "module": "dist/atjson.js",
  "types": "dist/atjson.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p . --module amd --outFile dist/atjson.js",
    "test": "jest"
  },
  "license": "Apache 2.0",
  "devDependencies": {
    "tslint": "^5.7.0",
    "typescript": "^2.4.2"
  }
}
