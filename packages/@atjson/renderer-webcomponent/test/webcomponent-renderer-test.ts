import Document, { Annotation } from '@atjson/document';
import WebComponentRenderer from '@atjson/renderer-webcomponent';

describe('PlainTextRenderer', function () {
  it('renders the atjson document');
});
